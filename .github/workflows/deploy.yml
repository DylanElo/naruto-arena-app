name: Deploy to GitHub Pages

on:
  push:
<<<<<<< HEAD
    branches: ['main']
=======
    branches: [main]
>>>>>>> 16eecc7e67cf3c064496e2ecdf1369c51a7f9048
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
<<<<<<< HEAD
      
      - name: Setup Node
=======

      - name: Setup Node.js
>>>>>>> 16eecc7e67cf3c064496e2ecdf1369c51a7f9048
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
<<<<<<< HEAD
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
=======

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_BASE_PATH: "/naruto-arena-app/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
>>>>>>> 16eecc7e67cf3c064496e2ecdf1369c51a7f9048
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
